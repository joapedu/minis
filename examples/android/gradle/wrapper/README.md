This folder contains Gradle wrapper configuration. To create the complete wrapper locally run:

```bash
cd examples/android
bash ./bootstrap-gradle-wrapper.sh
```

The bootstrap script will download the Gradle distribution and create a `gradle-wrapper/gradle-<version>/` tree and a `gradlew` wrapper script.
