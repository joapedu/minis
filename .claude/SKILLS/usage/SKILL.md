Prompt: Usage examples and conventions

Purpose: quick snippets for contributors showing patterns used in the repo.

Examples:
- Minimal `core::init()` usage.
- How platform `main` files should call into `core`.
- How to add a new example and wire it into CI.
